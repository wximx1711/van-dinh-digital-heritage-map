# =============================================================================
# Frontend Environment Variables (Vite)
# Set these in a .env file at the project root
# =============================================================================

# Note: The mapping subsystem uses Leaflet + OpenStreetMap.
# No API key required for maps or routing.

# =============================================================================
# Backend Environment Variables (ASP.NET Core)
# These override appsettings.Production.json values.
# Set as system environment variables or in the hosting platform's config.
# =============================================================================

# SQL Server connection string (required)
ConnectionStrings__DefaultConnection=Server=YOUR_SERVER;Database=VanDinhDigitalMap;User Id=YOUR_USER;Password=YOUR_PASSWORD;Encrypt=True;TrustServerCertificate=False

# Allowed CORS origins (space-separated or set via appsettings)
# Cors__AllowedOrigins__0=https://yourdomain.com
# Cors__AllowedOrigins__1=https://www.yourdomain.com

# Session idle timeout in hours (default: 2)
# Session__IdleTimeoutHours=2

# Auth cookie name (default: .VanDinh.Auth)
# AuthCookie__CookieName=.VanDinh.Auth

# CSRF cookie name (default: .VanDinh.Csrf)
# Csrf__CookieName=.VanDinh.Csrf

# Seed admin credentials (used on first startup to create admin user)
# WARNING: Override these via environment variables in production.
# The API REQUIRES both SeedAdmin and SeedManager on a fresh Production database.
# Do not rely on appsettings.Production.json defaults.
SeedAdmin__Username=admin
SeedAdmin__Password=your_secure_admin_password
SeedAdmin__FullName=System Administrator
SeedAdmin__Email=admin@vandinh.gov.vn

# Seed manager credentials (required on a fresh Production database)
SeedManager__Username=manager
SeedManager__Password=your_secure_manager_password
SeedManager__FullName=Heritage Manager
SeedManager__Email=manager@vandinh.gov.vn

# =============================================================================
# Docker Compose (docker-compose.yml — development stack with local SQL Server)
# =============================================================================

# SQL Server 'sa' password for the bundled database container
# MSSQL_SA_PASSWORD=YourStrong!Password

# Host port mapped to the nginx container (development default)
# FRONTEND_PORT=8080

# ASP.NET Core environment (set to Production in production)
ASPNETCORE_ENVIRONMENT=Production

# =============================================================================
# Docker Compose (docker-compose.azure.yml — production stack on Azure SQL)
# =============================================================================

# Azure SQL connection string (REQUIRED for docker-compose.azure.yml).
# Use Encrypt=True and TrustServerCertificate=False for Azure SQL.
# NEVER commit real credentials; put them in the local .env / host secrets.
CONNECTION_STRING=Server=tcp:YOUR-SERVER.database.windows.net,1433;Database=VanDinhDigitalMap;User ID=YOUR_USER;Password=YOUR_PASSWORD;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30

# Public https origin of the frontend (REQUIRED for docker-compose.azure.yml).
# Used as the only allowed CORS origin (allow-list; never AllowAnyOrigin).
# Example: https://map.vandinh.gov.vn
FRONTEND_ORIGIN=https://yourdomain.com

# Host port mapped to the nginx container in production (default: 80)
# FRONTEND_PORT=80

# Swagger route prefix in production (default: empty = disabled)
# SWAGGER_ROUTE_PREFIX=
